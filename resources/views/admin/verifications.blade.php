@extends('layouts.app')
@section('content')
<main class="mx-auto max-w-6xl px-4 py-10"><div class="mb-8 flex items-end justify-between border-b border-silver-800 pb-5"><div><p class="font-mono text-xs tracking-[.3em] text-silver-500">// ENFORCER REVIEW QUEUE</p><h1 class="font-oswald text-5xl uppercase">Verification documents</h1></div><a href="{{ route('admin') }}" class="border border-silver-600 px-3 py-2 text-xs">BACK TO ADMIN</a></div><div class="space-y-4">@forelse($verifications as $verification)<article class="border border-silver-700 bg-pitch-900 p-5 text-xs"><div class="flex flex-wrap justify-between gap-4"><div><h2 class="font-oswald text-2xl uppercase">{{ $verification->user->name }}</h2><p class="text-silver-500">{{ $verification->user->citizen_number }} // {{ strtoupper($verification->status) }}</p><p class="mt-3">Mobile: {{ $verification->mobile_number }} // Social: {{ $verification->social_handle }}</p>@if($verification->valid_id_path)<a class="mt-3 inline-block text-white underline" href="{{ route('admin.verifications.document', $verification) }}">DOWNLOAD VALID ID</a>@else<p class="mt-3 text-red-300">DOCUMENT FILE UNAVAILABLE</p>@endif</div><form method="post" action="{{ route('admin.verifications.update', $verification) }}" class="flex items-start gap-2">@csrf @method('PATCH')<select name="status" class="bg-pitch-950 p-2"><option value="approved">APPROVE</option><option value="rejected">REJECT</option></select><input name="admin_notes" placeholder="Review note" class="border border-silver-700 bg-pitch-950 p-2"><button class="border border-silver-500 px-3 py-2">SAVE</button></form></div></article>@empty<div class="border border-silver-800 p-10 text-center text-silver-500">NO VERIFICATION SUBMISSIONS.</div>@endforelse</div></main>
@endsection
